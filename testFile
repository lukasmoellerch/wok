prefix operator -
infix operator + 3 right
infix operator - 3 right
infix operator * 4 right
infix operator == 2 right
infix operator != 2 right
infix operator > 2 right
infix operator < 2 right
infix operator >= 2 right
infix operator <= 2 right

class A {
  var a: Int
  var b: Int
  func returnASquared(): Int {
    return self.a * self.b
  }
}

class B {
  let b: Int
}

struct List {
  let value: Int
  let next: UInt32
}

@foreign("printInt") func printInt(str: Int)
@foreign("print") func print(str: String)
let testVariable = 123
printInt(testVariable + 345)
func a(): Int {
  return 456 // asd
}
func b(): Int {
  return 123
}
@export("main") func main(max: Int) {
  let ainstance = A(123, 12)
  ainstance.a = 345 + testVariable
  ainstance.returnASquared()
  var q = a
  if (max < 10) {
    q = a
  } else {
   q = b
  }
  printInt(q())
}
extension Int {
  func addingTwo(): Int {
    return self + 2
  }
}